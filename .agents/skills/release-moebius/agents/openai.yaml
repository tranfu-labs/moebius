interface:
  display_name: "Release Moebius"
  short_description: "Release Moebius with verified macOS artifacts"
  default_prompt: "Use $release-moebius to recommend and confirm a version when needed, then publish the current main branch."
