# 任务：agent-avatar-team-navigation

- [x] 模块 A：补齐运行历史团队键、成员 Provider 投影和活动 run profile；增加对应纯逻辑测试。
- [x] 模块 B：让活动头像复用 run 信息 Popover；默认显示 Provider 标识；将历史 Markdown 动作替换为 Agent 详情导航；更新组件测试与文案。
- [x] 模块 C：接通 `teamKey + memberSlug` 的桌面导航意图；覆盖延迟 catalog、成员不存在和真实回调接线。
- [x] 模块 D：完成边界矩阵、写入 `spec-delta`、执行全量回归并核对产品事实源。
