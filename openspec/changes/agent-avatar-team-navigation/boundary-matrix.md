# 边界矩阵：agent-avatar-team-navigation

每个单元格都给出处理与验证位置；“复用”表示沿用已存在的处理和测试，不新增第二套状态机。

| 功能单元 | 空输入 | 非法或超限输入 | 并发或重入 | 无权限 | 失败恢复 |
| --- | --- | --- | --- | --- | --- |
| A. 运行事实与 Provider 投影 | 缺少团队快照或执行 profile 时投影为 `null`，头像不伪造 Provider 或导航目标；复用 `tests/local-console-run-agent-audit-plan.test.ts`、`tests/local-console-run-lifecycle-activity.test.ts`。 | 快照/成员身份解析失败沿用现有规范化与 fail-closed 处理，不从当前团队回填；复用 `tests/local-console-member-identity.test.ts`、`tests/local-console-agent-team-snapshot-migration.test.ts`。 | profile 按 `(sessionId, runId)` 随 run 快照投影，迟到响应隔离复用 B 的 run-scoped loader；复用 `packages/console-ui/src/console/agent-run-info-popover.test.tsx`。 | 运行事实层不新增权限判断；团队详情已有的编辑能力边界继续由 `canEditContent` 等既有字段控制；复用 `packages/console-ui/src/console/agent-team-detail.test.tsx`。 | 缺失 profile/teamKey 显示未记录并隐藏错误跳转；信息读取失败沿用 Popover 局部错误/重试；复用 `agent-run-info-popover.test.tsx`。 |
| B. 共享头像与 Agent 信息 Popover | 没有 `runId` 或信息 loader 时降级为静态 `RoleTag`；复用 `packages/console-ui/src/console/run-block.test.tsx`。 | 缺失/未知 profile 显示“此项未记录”，不读取当前团队值；复用 `packages/console-ui/src/console/agent-run-info-popover.test.tsx`。 | 每个 Popover 以 session/run 隔离请求，重入、取消和迟到结果沿用现有 Abort/focus 处理；复用 `agent-run-info-popover.test.tsx`。 | 缺少历史 `teamKey` 或宿主语义回调时不渲染详情按钮；复用 `agent-run-info-popover.test.tsx`。 | loader 失败保留局部错误与重试，Popover 关闭后焦点回到原头像；复用 `agent-run-info-popover.test.tsx`。 |
| C. 桌面团队成员原子导航 | 指定成员不存在时打开团队但使用 `memberSlug: null`，不猜测成员；复用 `desktop/tests/use-agent-team-navigation.test.tsx`。 | 团队尚未进入 catalog 时保留目标；团队存在但成员缺失时安全降级；复用 `use-agent-team-navigation.test.tsx`。 | catalog 到达前重复导航只保留最新成员目标，避免旧意图覆盖新点击；复用 `use-agent-team-navigation.test.tsx` 的 re-entry 用例。 | 导航只打开既有详情页，编辑/删除等动作继续由团队页现有能力字段控制；复用 `packages/console-ui/src/console/agent-teams-page.test.tsx`、`agent-team-detail.test.tsx`。 | catalog 延迟后自动兑现目标；成员内容加载失败沿用 member editor 的局部错误/重试；复用 `use-agent-team-navigation.test.tsx`、`desktop/tests/use-agent-team-member-editor.test.tsx`。 |
| D. 真实 Electron 闭环 | 旧记录没有稳定身份时复用 B 的静态/无跳转降级；无需新增页面空态。 | 历史团队或成员目标不可解析时复用 A/B/C 的安全降级，不跳当前团队。 | 活动头像与历史头像分别打开同一 run-scoped Popover；复用真实验收 `--case agent-avatar-team-navigation`。 | 详情页沿用既有团队权限/能力呈现，不在 Popover 内复制权限逻辑；复用 C 的页面测试。 | 真实验收覆盖活动头像可点击、Provider 标识、历史跳转并预选成员；加载失败恢复复用 B 的测试。 |
