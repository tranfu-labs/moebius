# 线框：add-byok-pi-agent-runtime

历史版式基线：`docs/wireframes/pages/onboarding.md` 与 `docs/wireframes/pages/console.md`。五个页面已经由 `docs/product/pages/` 接管产品结构，因此本 change 只记录已确认 BYOK 流程在这些版式中的新增区域；归档时回流各页面 PRD，不向历史 wireframe 建立第二事实源。高保真状态参考 `docs/product/flows/byok-agent-runtime.prototype.html`，生产不得导入或复制其实现。

## pages/onboarding.md

### 第 1 步 · API-only 环境就绪

```text
┌──────────────────────────────────────────────────────────────┐
│ Moebius                                      步骤 1 / 4       │
├──────────────────────────────────────────────────────────────┤
│ 让至少一个执行环境就绪                                      │
│                                                              │
│ Codex        未安装      Claude Code  未安装      Kimi 未安装 │
│                                                              │
│ AI 服务商                                                   │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ DeepSeek       已就绪                                   │ │
│ │ deepseek-v4-pro · Key …7K2M                [管理]       │ │
│ └──────────────────────────────────────────────────────────┘ │
│ [+ 添加 API 服务商]                                         │
│                                                              │
│                                          [继续选择团队]      │
└──────────────────────────────────────────────────────────────┘
```

### 添加与验证

```text
┌────────────────────────────────────────────┐
│ 添加 AI 服务商                         ×  │
│ 服务商  [ DeepSeek                  ▾ ]   │
│ API Key [ •••••••••••••••••••••••     ]   │
│ 模型     [ deepseek-v4-pro          ▾ ]   │
│ 数据将发送给 DeepSeek；测试产生少量用量。 │
│                                            │
│ 验证模型回复        ✓                      │
│ 验证受控工具调用    进行中…                │
│                         [取消] [验证并保存] │
└────────────────────────────────────────────┘
```

窄窗口中弹层变为全宽单列，说明、进度和按钮不被裁切；迟到验证不得覆盖重新打开后的另一份草稿。

## pages/settings.md

### AI 服务商列表与详情

```text
┌──────────────────────────────────────────────────────────────────┐
│ 设置                                                         × │
├──────────────┬───────────────────────────────────────────────────┤
│ 常规         │ AI 服务商                          [+ 添加]       │
│ AI 服务商  ● │                                                   │
│ 关于         │ DeepSeek · 生产账号                  已就绪       │
│              │ deepseek-v4-pro · 2 个已验证模型 · Key …7K2M    │
│              │ [管理模型] [替换 Key] [停用] [更多 ▾]            │
│              │                                                   │
│              │ DeepSeek · 旧账号                  需要处理       │
│              │ 服务商目录已移除                                   │
│              │ [添加新档案并迁移] [结束继续能力]                  │
└──────────────┴───────────────────────────────────────────────────┘
```

### 引用保护

```text
┌──────────────────────────────────────────────┐
│ 无法移除 deepseek-v4-pro                     │
│ 仍有 4 个引用：                              │
│ 团队成员 2 · 建队草稿 1 · 可恢复会话 1       │
│ [查看逐项引用]                               │
│                         [取消] [先迁移引用]   │
└──────────────────────────────────────────────┘
```

窄窗口隐藏左侧分类栏为顶部选择器；引用表改卡片列表，危险操作保持在最后且不可与取消重叠。

## pages/agent-teams.md

### 成员 Pi 配置

```text
┌──────────────────────────────────────────────────────────────┐
│ 开发 Agent                                                   │
│ 执行引擎  [ Pi API                                      ▾ ] │
│ AI 服务商 [ DeepSeek · 生产账号                          ▾ ] │
│ 模型       [ deepseek-v4-pro                             ▾ ] │
│ 思考程度   [ 高                                          ▾ ] │
│                                                              │
│ 该配置会被新会话冻结；Key 由应用安全管理。       [保存成员] │
└──────────────────────────────────────────────────────────────┘
```

默认模型下架时 Provider 保留，模型为空并显示“请选择另一个已验证模型”，保存不可用，不自动选择。

## pages/main-conversation.md

### Pi 运行与恢复卡片

```text
┌────────────────────────────────────────────────────────────────┐
│ 开发 Agent · Pi API · deepseek-v4-pro              正在工作   │
│ 计划  读取实现 → 修改文件 → 运行测试                          │
│ 当前  运行 pnpm test …                                        │
│                                              [停下] [查看过程] │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│ 原服务商已无法继续                                             │
│ 历史仍可阅读；继续会建立新的 Pi 上下文。                       │
│                       [迁移到已就绪档案] [结束并保留历史]      │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│ 未发送 · 原目标不可继续                                        │
│ “继续修复剩余两个测试……” · 附件 1                             │
│ 原目标：开发 Agent                         [编辑并重提] [移除] │
└────────────────────────────────────────────────────────────────┘
```

窄窗口把过程详情放入全宽 sheet；恢复卡片动作纵向排列，未发送项目不遮挡 composer 或团队切换。

## pages/agent-conversation.md

### 安全完整输出

```text
┌──────────────────────────────────────────────────────────────┐
│ 开发 Agent / 尝试 2                          [返回主对话]    │
├──────────────────────────────────────────────────────────────┤
│ 计划                                                         │
│ ✓ 读取文件   ✓ 修改文件   ● 运行测试                         │
│                                                              │
│ 最新活动                                                     │
│ pnpm test · 18 项通过                                        │
│                                                              │
│ 系统事实                                                     │
│ 已整理较早上下文                                             │
│                                                              │
│ [展开完整输出]  仅显示安全清洗后的详细过程                   │
└──────────────────────────────────────────────────────────────┘
```

失败展开只显示安全分类与恢复入口；Key、Authorization、原始 Provider error body、请求响应载荷、内部 frame 和绝对路径均不进入可复制区域。
