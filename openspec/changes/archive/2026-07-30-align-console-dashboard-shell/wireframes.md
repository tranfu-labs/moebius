# 版式：align-console-dashboard-shell

基线：`docs/wireframes/pages/console.md`。该文件是历史线框；页面职责和行为冲突时仍以
`docs/product/pages/main-left-sidebar.md` 与 `docs/product/pages/main-conversation.md` 为准。
本次高保真视觉对照为 `packages/console-ui/design-refs/dashboard.html` 及同目录 `app.css`。

## pages/main-left-sidebar.md

```text
┌──────────── 默认 252px ────────────┐
│ ● ● ●                 [关闭] │ 46px 窗口控制行
│ [Logo] Moebius                 │ 34px 品牌行
│                                │
│ [icon] 新建对话                │ 34px
│ [icon] 搜索                    │ 34px
│ [icon] Agent 团队              │ 34px
│                                │
│ 项目                           │ 11.5px 区标题
│ ▾ 项目 A              [＋][⋯] │ 32px 项目行
│   » 当前会话                 ● │ 32px 会话行，左缩进 28px
│     另一会话                 ● │
│ ▸ 项目 B                  ●   │ 折叠聚合点
│                                │ 项目列表独立滚动
├────────────────────────────────┤
│ [icon] 重新查看引导            │ 34px，生产能力保留
│ [icon] 设置                    │ 34px
└────────────────────────────────┘
```

右边界仍可拖动；252px 只是默认值，不把参考稿的固定宽度覆盖到产品的可调整规则。
侧栏与主区使用同一 canvas；右边界和底部操作顶部是 1px 语义分隔线。窗口控制、品牌、应用导航
和底部操作不滚动，只有中间项目列表滚动；不增加阴影、渐变或裸色。

## pages/main-conversation.md

```text
┌────────────────────────── 主会话可用宽度 ──────────────────────────┐
│ [开侧栏]                                              [开右栏] │ 46px
├────────────────────────────────────────────────────────────────────┤
│       ┌────────────── 最大 840px，窄时左右 32px ──────────────┐   │
│       │ 会话标题（sticky 46px）                              │   │
│       │                                                      │   │
│  轨   │                         时间 · 你 · 24px 头像        │   │
│  道   │                         ┌─ 用户气泡，最大 75% ─┐     │   │
│       │                         └───────────────────────┘     │   │
│       │ 24px 头像 · Agent · 耗时                            │   │
│       │     Agent 正文：左缩进 32px，最大 68ch              │   │
│       │                                                      │   │
│       │ 24px 头像 · 活动 run                         操作   │   │
│       │     最新安全活动                                    │   │
│       └──────────────────────────────────────────────────────┘   │
│       ┌────────────── 同一 840px 内容轴 ────────────────────┐   │
│       │ [项目] [工作空间] [分支] [团队]                    │   │
│       │ 说点什么…                         [＋] [↑/■]       │   │
│       └──────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────┘
```

未画出的右侧栏不属于本 change；右侧栏开关仍保留，打开后只压缩主会话可用宽度，不继承主会话
840px 内容轴或 main composer 变体。

主区顶部控制行、sticky 标题和底部 composer 不随消息离开视口，只有中间时间线滚动；时间线
底部保留空间，确保最后一条消息不被不透明的 composer 区域遮挡。该关系不限定具体 CSS 定位方式。
