# 任务：align-main-composer-column

- [x] 新增主会话正文列共享最大宽度类，并接入标题、时间线、待发射区和已有会话 composer。
- [x] 让新对话标题与 composer 宿主复用同一宽度类，保持窄窗口 `w-full` 收缩。
- [x] 增加现有会话、待发射区和新对话宽度契约测试，并确认右侧子任务输入框未被改动。
- [x] 运行目标 Vitest、`pnpm typecheck` 与 `@moebius/console-ui` build。
- [x] 按 PRD 字符图核对宽窗左右边界一致、窄窗无页面级横向溢出。
