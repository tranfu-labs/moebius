# 路线 1 交叉验收 · 路线 3 审查者

## 第一轮

### PASS

- 审查日期：2026-07-18
- 审查范围：`sites/marketeam/style1.html`、`sites/marketeam/index1.html`
- 目标图：`/Users/wing/.codex/generated_images/019f70b3-b15a-7f91-b5dd-2b932bef26e6/exec-1138557c-94f9-419b-b5eb-44083aa73a8b.png`
- 视口：desktop 1440×1000、tablet 1024×768、mobile 390×844
- 结论：未发现 P0 / P1 / P2，路线 1 通过交叉验收。

## 路线定义与目标图

- 六行运行底单是首屏第一视觉主角，时间、负责人、动作、状态、证据形成稳定列；不是普通营销首屏换皮。
- 实现保留目标图的“纸上运行记录”气质，但没有复制 Kimi 的蓝色双行标题、研究报告封面、芯片爆炸图、胶囊章节导航或点阵画布。
- `style1.html` 是可操作样张；`index1.html` 使用完整 Moebius 产品叙事，不含“视觉令牌、样张、设计方向”等面向用户的设计说明。
- `index1.html` 覆盖为什么需要、责任转移、对齐、验收、返工、真人边界、过程底单与开始使用，叙事完整。

## 路线 1 特有判据

- 六行均包含时间、负责人、动作 / 交接、状态、证据；桌面可横向扫读，窄屏改为同一行内部的三段纵向排布，没有丢字段。
- 第 3 行明确写出退回原因“移动端证据链不完整”与“退回 #02”；第 2 行写出“来自 #03 的重做”。两行通过编号与双向文案建立明确关系。
- 悬停、焦点或当前行会展开深色关系条。例如第 3 行显示退回原因随任务返回第 2 行；关系不只靠红色表达。
- 第 5 行明确关闭原退回原因并复核通过；第 6 行带菱形状态标记、结构左边界和“真人决策”文字，与自动路径使用非颜色形状区分。
- 最终行附 `交付包 v1.0`、决策记录和 12 条证据，交付结果可定位。
- 时间轴会同步更新当前行、未来行弱化、`aria-pressed`、进度滑杆、状态读数和关系条。实测选择第 3 行后：当前步骤为 `2`、未来行数量为 `3`、关系条为可见；播放 1.3 秒后进入第 4 行，读数变为“09:37 · 定点重做”。
- `?step=N` 可冻结具体状态，行按钮和滑杆也可直接冻结；满足逐步推进、回退、播放和固定状态验收。

## 浏览器证据

| 页面 / 视口 | viewport | document scrollWidth | 主要结果 |
|---|---:|---:|---|
| `style1.html` desktop | 1440 | 1425 | 6 行；当前行、关系条、播放与回退工作正常 |
| `style1.html` tablet | 1024 | 1009 | 底单宽 937；控制器均在视口内 |
| `style1.html` mobile | 390 | 375 | 底单宽 351；6 行宽 349；页面无横向滚动 |
| `index1.html` desktop | 1440 | 1425 | 正式叙事与 6 行首屏底单完整 |
| `index1.html` tablet | 1024 | 1009 | 底单宽 937；收据宽 555；无遮挡 |
| `index1.html` mobile | 390 | 375 | 底单与收据均宽 351；导航内部也无额外滚动 |

- 390×844 下时间轴的播放按钮为 44px 高，滑杆宽 325px，状态输出宽 219px；主要控件没有被裁切。
- 移动端隐藏独立“上一步”按钮，但保留滑杆、播放与六个可直接激活的行，可通过滑杆和行按钮前进或回退。
- 4 条过程收据使用原生 `details/summary`。实测展开第 2 条后，`open=true`、`aria-expanded=true`、`aria-controls=d2`，详情可见。
- 行和控制器均是原生按钮 / 滑杆，键盘可达；`:focus-visible` 有 3px 结构焦点框，行聚焦同步显示路径关系。

## reduced-motion、no-JS、控制台与资源

- `prefers-reduced-motion: reduce` 关闭全部动画和过渡，移除 live 扩散阴影；脚本默认直接冻结在第 6 行终态，不强制自动播放。
- no-JS 下 HTML 本身保留六行、角色、退回、通过、真人决策与最终交付；第 6 行为稳定终态，并显示“静态终态”说明。
- 两页内联脚本均通过语法编译检查，无重复 `id`，`git diff --check` 通过。
- 两页 HTTP 返回 200；没有外部样式、脚本、SDK 或业务数据资源，因而没有页面资源 404。
- 浏览器交互未观察到页面脚本异常。浏览器扩展自身产生的连接与消息流警告不属于页面代码，已从页面错误判断中排除。
- 审查前后未修改路线 1 文件；验收哈希：`style1.html` 为 `d76dd5bd08805a9c8fca9f3da2293304d84ef77f`，`index1.html` 为 `12b25db85cd2bc958456712016a47f1fd06ba0f8`。

## 非阻断 P3

1. reduced-motion 模式下从第 6 行点击“播放”，脚本会先回到第 1 行再阻止定时播放；全部信息仍可见且可用滑杆恢复，但按钮反馈可以更明确，例如改成“逐步查看”或在该模式下不先重置。
2. 窄屏隐藏了桌面版“退回至 #02 / 来自 #03 的重做”角标；动作与证据列仍保留原因和 `返回 #02`，不影响理解，但可考虑用更紧凑的结构符号强化双向关系。
3. 与目标图相比，实现用“当前行 + 深色关系条”表达路径，而非绘制贯穿六行的独立线轨；语义已达到验收要求，但在快速扫视时的路径连续感略弱。

**final result: passed**
