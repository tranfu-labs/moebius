# 路线 1 / 路线 3 精确视觉还原审计

## 裁决原则

**最初生成图是唯一视觉事实源。** 本轮不再以“概念相似”“叙事成立”或“路线特征存在”替代像素级还原。主区域的位置、比例、首屏信息量、字体尺度、线条密度和中心作品结构，均以最初图为准。

- 路线 1 目标：`exec-1138557c-94f9-419b-b5eb-44083aa73a8b.png`，1487×1058。
- 路线 1 当前：`style1-before.png`，1473×1047。
- 路线 3 目标：`exec-973aad59-ddca-4362-a715-6f038cc415e3.png`，1487×1058。
- 路线 3 当前：`style3-before.png`，1473×1047。
- 两组图像宽高差均小于 1%，以下原生像素坐标可以直接比较；百分比用于消除 14×11 px 的截图尺寸差。
- 本轮结论：路线 1 `REWORK`；路线 3 `REWORK`。两者均存在 P1 级构图与主作品结构偏差。

---

## 路线 1 · 运行中底单

### [P1] 首屏信息架构被额外巨型宣言区改写

- 目标图：页眉约 `y=0–62`；任务标题直接出现在 `y≈108–139`；底单主体从 `y≈201` 开始。任务标题到表头之间只有约 60 px 的任务说明区。
- 当前图：页眉约 `y=0–63`；新增宣言区占据 `y≈113–393`；底单主体直到 `y≈424` 才开始。
- 量化偏差：底单起点由目标的约 `19.0%H` 下移到当前的约 `40.5%H`，垂直位移约 `+223 px / +21.5%H`。
- 可观察结果：目标在首屏同时显示六行任务、底部时间轴和下一节令牌入口；当前首屏只能看到底单前五行的一部分，时间轴和下一节完全不在视口内。
- 修复要求：移除目标图不存在的巨型宣言区。标题、说明、操作按钮应回到目标 `y≈108–170` 的紧凑任务头；底单上沿必须回到 `y≈200±12`。

### [P1] 中心作品从“六行完整运行底单”变成“带展开黑带的普通表格”

- 目标结构：`y≈252–731` 内完整展示六行，平均每行约 `79–80 px`；蓝色连续运行轨迹位于 `x≈803`，贯穿六行，第三行红色回路从评审返回方案设计，第五行转绿，第六行以真人决策菱形结束。
- 当前结构：首行后插入约 `52 px` 高的深色展开带（约 `y=667–719`）；连续竖向运行轨迹、红色回路、绿段与末端真人菱形没有作为表格中央的第一视觉主线出现；下方行被推出首屏。
- 影响：目标的核心阅读顺序是“六行记录 → 中央路径关系 → 状态与证据”；当前变为“巨型标题 → 表头 → 单行展开详情”，主作品模型和首屏扫描路径均不同。
- 修复要求：按目标恢复六行等密度底单与中央连续路径。首屏默认状态不得插入目标图没有的深色横向详情带；若保留展开交互，默认必须收起且不能改变目标首屏行高。

### [P2] 画布有效宽度和列密度偏小

- 目标底单约从 `x≈49` 到 `x≈1437`，有效宽度约 `1388 px / 93.3%W`。
- 当前底单约从 `x≈76` 到 `x≈1383`，有效宽度约 `1307 px / 88.7%W`。
- 偏差：左右合计少约 `81 px`，有效宽度少约 `4.6%W`；动作、状态、证据列因此更拥挤，目标图的横向审计感减弱。
- 修复要求：桌面 1487 基准下把主体左右边界收敛到 `x≈49±10` 与 `x≈1437±10`，并按目标列宽重新分配时间、步骤、负责人、动作、状态、证据。

### [P2] 标题字号、层级和页面密度不匹配

- 目标任务标题约 `31–34 px`，一行完成；说明约 `14–15 px`。标题只是底单的任务头，不是页面 hero。
- 当前宣言标题视觉高度约两行 `140 px`，单字约 `58–66 px`；底单内部标题又约 `38–42 px`，形成重复主标题。
- 偏差：当前最大标题约为目标任务标题的 `1.8–2.0×`，并多出一层目标不存在的一级层级。
- 修复要求：删除重复 hero 层级；首屏最大标题控制在目标约 `32 px`，底单编号、状态和说明按目标一行排布。

### [P2] 状态颜色与路径权重未达到目标的辨识强度

- 目标以高纯度蓝色贯穿运行主线与“对齐中”，红色只用于退回路径和“已退回”，绿色只用于通过节点和“已通过”；三色路径是首屏中最强的非文字信号。
- 当前首屏除第一行蓝色外，大部分状态降为灰色，红/绿关系没有形成同等强度的中央路径。
- 修复要求：以目标采样关系恢复蓝主线、红回路、绿通过段的线宽和对比度；不能只在标签文字中保留语义色。

### 路线 1 精确复验门槛

1. 底单上沿 `y=200±12`，不再存在额外巨型 hero。
2. 六行、真人菱形、时间轴均在 1058 px 高首屏内完整可见。
3. 六行区域高度约 `478±20 px`，默认无额外深色展开带。
4. 主体左右边界接近 `x=49±10 / 1437±10`。
5. 蓝主线、红退回、绿通过和真人末端与目标位置关系一致。
6. `Visual Tokens` 分隔线与标题在 `y≈828–913` 范围进入首屏。

---

## 路线 3 · 证据织机

### [P1] 首屏被放大的两行 hero 占据，整个织机下移

- 目标图：页眉约 `y=0–58`；标题位于 `x≈42, y≈100–138`，单行，约 `36–40 px`；五行说明位于 `y≈162–247`；织机从 `y≈282` 开始。
- 当前图：页眉约 `y=0–67`；新增 eyebrow 位于 `y≈115`；标题位于 `y≈151–272`，拆成两行，约 `58–64 px`；织机从 `y≈393` 才开始。
- 量化偏差：织机起点由约 `26.7%H` 下移到约 `37.5%H`，约 `+111 px / +10.8%H`；标题区域高度约为目标的 `2.7–3.0×`。
- 可观察结果：目标首屏能够完整容纳织机、真人裁决条、三声部和页脚；当前首屏在控制器与裁决卡处截断，三声部和页脚完全不可见。
- 修复要求：标题恢复目标的一行尺度与 `y≈100` 起点；删除目标图不存在的 eyebrow 占位；织机上沿回到 `y≈282±12`。

### [P1] 证据织机的经纬结构方向错误，当前只是“四列流程图”

- 目标主作品：角色标签沿左侧纵向分布，约位于 `y≈337 / 421 / 531 / 642`；每个角色形成横向经纬带。大量细密米色证据线从 `x≈105` 贯穿到 `x≈1125`，卡片集中在 `x≈552–879`；深色主任务线纵向弯折穿过角色带。
- 当前主作品：角色被放到顶部四列，约位于 `x≈160 / 420 / 684 / 947, y≈477`；证据卡以宽盒从左上向右下排列，背景是稀疏表格线；缺少目标图从左至右持续贯穿的高密度纤维束。
- 影响：虽然都有“卡片 + 曲线”，但目标的“角色经线 / 证据纬线 / 任务主线”三层关系被改成了常规泳道流程图，属于主作品结构级偏差。
- 修复要求：严格恢复目标方向：四个角色作为左侧纵向分区；每个角色对应一组横向证据纤维；卡片落在目标中部区域；主任务线按目标从人类目标向下穿越四个角色区。

### [P1] 真人决策与交付收据的空间关系错误

- 目标：交付收据从 `x≈1125` 的竖线开始，约占右侧 `310 px`，顶端从 `y≈140` 开始；真人决策边界位于主作品下方 `x≈315–873, y≈742–808`，是织机路径的直接末端。
- 当前：收据从 `x≈1102` 开始但与织机同在 `y≈393` 才出现，缺少目标上半部约 `250 px` 的收据内容；真人决策卡位于右栏 `x≈1130, y≈928`，从主任务线末端移到了收据下方，并在首屏被截断。
- 影响：目标明确表达“自动织造在主线末端停下，由人裁决；收据在旁持续汇总”。当前把真人裁决并入右侧信息栏，破坏决策边界与任务主线的因果关系。
- 修复要求：收据顶边回到 `y≈140`；真人决策横条恢复到主任务线末端 `x≈315–873, y≈742–808`，不得放在右侧收据栏底部。

### [P1] 目标首屏的三声部与证据织纹缺失

- 目标在 `y≈849` 出现分隔线，`y≈878–973` 同屏展示判断、解释、记录与证据即织物，页脚在 `y≈1000`。
- 当前在 `y≈925` 仍是织机控制器和裁决卡，三声部整体位于首屏之外。
- 修复要求：压缩上部与织机至目标高度，确保 1058 px 截图内出现完整三声部和底部页脚；不能仅靠继续向下滚动补齐。

### [P2] 字号、段落行数和留白节奏不匹配

- 目标标题单行约 `36–40 px`；说明约 `15–17 px`、五行、宽约 `500 px`。
- 当前标题约 `58–64 px`、两行；说明约 `18–20 px`、两行、宽约 `740 px`。
- 当前把目标的“编辑式紧凑说明 + 主作品”改成“大标题 + 宽段落 + 主作品”，导致纵向密度显著降低。
- 修复要求：按目标控制标题和说明的字号、行宽与换行；首屏介绍区总高度应约 `220 px`，不能超过约 `300 px`。

### [P2] 证据纹理密度不足

- 目标每个角色带中存在多组相邻细线，卡片右侧还有连续密织纹理，肉眼呈现“织物”而不是单条连接线。
- 当前主要依靠六条横线、四列竖线和一条粗曲线，细线数量级明显低于目标。
- 修复要求：以目标可见密度为准增加真实经纬线束；1487 px 宽截图下，角色带内应能分辨成组细线而不是单根分隔线，同时保持卡片文字可读。

### 路线 3 精确复验门槛

1. 标题回到单行 `y≈100–138`，织机上沿 `y=282±12`。
2. 角色按目标在左侧纵向分布，不再作为顶部四列表头。
3. 横向证据纤维、中央卡片和纵向弯折任务线的方向、密度与目标一致。
4. 收据顶端接近 `y≈140`；真人决策横条回到 `x≈315–873, y≈742–808`。
5. 三声部从 `y≈849` 开始并在首屏完整可见，页脚接近 `y≈1000`。
6. 首屏不得以滚动后补齐目标图原本同屏呈现的内容。

---

## 第二轮复验

### 路线 1 · 第二轮 PASS

- 复验依据：最初目标图 `exec-1138557c-94f9-419b-b5eb-44083aa73a8b.png`、修后并排图 `style1-source-vs-v3.png`、实页 `sites/marketeam/style1.html`。
- 实页复验视口：1487×1058；移动复验：390×844。
- 结论：路线 1 第一轮所有 P1 / P2 已关闭，未发现新的 P1 / P2。路线 1 精确视觉复验 `PASS`。

#### 六个精确门槛关闭证据

1. **底单位置**：实页 `.ledger` 为 `x=50, y=201, w=1387`，底单上沿命中 `y=200±12`；目标图不存在的巨型 hero 已移除。
2. **首屏完整性**：六行区域为 `y=252–729`，时间轴为 `y=729–827`；第六行真人菱形与时间轴完整可见。
3. **六行密度**：`.rows` 高 `477 px`，每行 `79.5 px`，命中目标约 `478±20 px`；默认没有深色展开带。
4. **主体边界**：实页主体为 `x=50–1437`，分别命中目标 `x=49±10 / 1437±10`；有效宽度 `1387 px`。
5. **关系路径**：连续轨道位于 `x=803.14`、高 `477 px`；红色退回回路为 `x≈703–773, y≈371–462`，第五节点转绿，第六行以真人菱形结束。并排图中蓝主线、红回路、绿通过段和末端菱形与目标位置关系一致。
6. **令牌进入首屏**：令牌区分隔线上沿 `y=827`，标题为 `x=50, y=881`，命中目标 `y≈828–913`；色板、字体、线条和间距样本均在首屏出现。

#### 字体、边距、颜色与线权

- 任务标题实测 `31 px / 37.2 px`，单行，目标约 `31–34 px`；令牌标题 `29 px / 36.25 px`。
- 页眉高 `64 px`，任务说明区高 `137 px`，与目标约 `62 px / 139 px` 的差异仅 2 px，未改变节奏或换行。
- 正文基础字号 `14 px / 21 px`；负责人、动作与证据主文均为 14 px，辅助记录为 12 px，表格密度与目标一致。
- 蓝 `#1f5cff`、红 `#ef4e2f`、绿 `#139a72` 分别承担主线/对齐、退回、通过；中性线为 `#ced0cf`，背景为 `#f7f6f3`。语义色没有扩散到无关区域。
- 轨道主段 2 px、细分隔线 1 px、节点 18 px、真人菱形 25 px；并排图未见会改变层级的线权偏差。

#### 核心交互与运行验证

- 初始第六行 `aria-pressed="true"`；点击第三行后活动步、滑杆和时间同步为 `2 / 09:27`。
- 键盘激活“下一步”后活动行变为第 4 行，焦点同步到 `data-step="3"` 的记录。
- 播放从终态正确重置到第 1 行并继续推进，`aria-pressed` 在播放/停止间同步切换。
- 自动跟随按钮可切换至 `aria-pressed="false"`；滑杆、前后步和行点击均可操作。
- 1487×1058 与 390×844 的页面级横向溢出均为 `0`；390 下控制按钮完整可见且高度为 `44 px`。
- `prefers-reduced-motion: reduce` 下过渡时长为 `0s`。
- 桌面和移动复验期间控制台无页面自身 error，资源无 404。

#### 路线 1 复验裁决

**final result: passed**

### 路线 3 · 第二轮

#### REWORK

- 复验依据：最初目标图 `exec-973aad59-ddca-4362-a715-6f038cc415e3.png`、修后并排图 `style3-after-comparison.png`、实页 `sites/marketeam/style3.html`。
- 实页复验视口：1487×1058；移动复验：390×844。
- 结论：第一轮构图、方向和密度类 P1/P2 已关闭，但仍有 2 项目标图不存在的静态界面 chrome，构成 P2。路线 3 第二轮仍为 `REWORK`，总结果保持待定。

#### 六个精确门槛复验

1. **标题与织机位置通过**：标题为 `x=42, y=100, h=39.6`，字号 `36 px / 39.6 px`；织机面板上沿 `y=278`，命中 `282±12`。
2. **角色方向通过**：定义、执行、评审、整合恢复为左侧纵向角色带，顶部约为 `y=326 / 401 / 514 / 620`；不再是四列表头。
3. **经纬和主线结构通过**：卡片起点 `x=551.6`、宽 `327 px`；深色任务线按目标向下穿越角色带，红色回线重新进入执行段，第五段转绿；每条证据右侧绘制 19 条细纬线，方向和密度达到目标。
4. **收据与真人边界通过**：收据为 `x=1125, y=138, w=320`；真人决策条为 `x=313.6, y=742, w=558, h=68`，与目标关系一致。
5. **三声部与页脚通过**：三声部为 `y=849–999`，页脚为 `y=999–1038`，均在 1058 px 首屏完整出现。
6. **首屏完整性通过**：标题、织机、交付收据、真人边界、三声部与页脚全部同屏，不依赖滚动补齐。

#### [P2] 终态证据卡出现目标图没有的高对比双重选中框

- 文件与位置：`sites/marketeam/style3.html` 的 `.evidence.current`，默认终态为 `.e6.current`。
- 可观察证据：目标图六张证据卡均使用约 1 px 的米灰边线，最后一张卡没有黑色选中框；修后并排图右侧最后一张“整合智能体 · 集成摘要.md”出现明显深色双重轮廓。
- 实页证据：`.e6` 本体为 `x=551.6, y=634, w=327, h=46`，除 1 px 卡片边框外又应用 `outline: 2px solid var(--ink)` 与 `outline-offset: 2px`。它成为织机中最重的矩形，改变目标的卡片层级和视觉落点。
- 修复要求：默认第六步不得给卡片加目标图不存在的深色 outline。当前步骤可以继续用路径终点、`aria-pressed` 和收据状态表达；若交互需要卡片反馈，仅在真实 `:focus-visible` / hover 时暂时出现，静态终态截图必须恢复目标的单线卡片。

#### [P2] 织机右上角常驻目标图不存在的推进控件

- 文件与位置：`sites/marketeam/style3.html` 的 `.loom-tools`。
- 可观察证据：目标图在织机右上方、收据左侧没有按钮；修后并排图右侧 `x≈950–1107, y≈281–306` 出现“← / 播放 / → / 06/06”四项控件轮廓。
- 实页证据：控件组尺寸 `157×25 px`，默认 `opacity: .24`。降低透明度仍留下四个可辨认的按钮/数字框，属于目标之外的静态 chrome；该空白区在目标图中用于保持织线呼吸感。
- 修复要求：保留核心交互，但静态默认态必须不可见，例如默认 `opacity:0`，仅在 `.loom-stage:hover` 或 `.loom-tools:focus-within` 时出现；键盘 Tab 到任一控件时必须恢复可见焦点。不得删除交互或把控件移到目标的真人决策条内。

#### 已通过的字体、边距、颜色、线密度与运行验证

- 页眉内容为 `x=42–1445, y=20–58`；主标题、说明、收据和中心作品的左右边距与目标一致。
- 正文基础 14 px；说明 `14 px / 23.8 px`；证据卡主文字 11 px、记录 9 px，密度与目标一致。
- 主任务线使用深蓝墨色，退回为红褐，复核通过为绿色，证据经纬为低对比米色；语义颜色和目标对应。
- 证据卡位置与尺寸、细纬线数量、角色分区和路径线权已达到目标；除上述默认 outline 外未见新的卡片层级偏差。
- 上一步、下一步、播放/暂停、证据卡定位、收据链反查和真人决策均可操作；播放状态 `aria-pressed` 正确切换，底单定位后焦点进入对应证据卡。
- 390×844 页面级横向溢出为 `0`；织机局部横向浏览宽差约 `538 px`，没有扩散为页面滚动。
- `prefers-reduced-motion: reduce` 下过渡为 `0s`；桌面和移动控制台无页面自身 error，资源无 404。

#### 路线 3 下一轮复验点

1. 静态第六步证据卡恢复目标的单线边框，不再有深色 outline / offset。
2. 织机推进控件在默认截图中完全不可见，hover 与键盘 focus 时仍可操作且焦点可见。
3. 复验以上改动未影响六个已通过的精确门槛、390 页面宽度和控制台。

---

## 路线 3 · 第三轮曲线专项复验

### REWORK

- 复验依据：最初目标图 `exec-973aad59-ddca-4362-a715-6f038cc415e3.png`、作者同尺寸截图 `artifacts/acceptance/style3-curve-final-1487x1058.png`、实页 `sites/marketeam/style3.html`。
- 实页与目标均为 `1487×1058`；另制作曲线局部同尺度并排裁剪 `/tmp/style3-curve-crop.png` 观察切线、直段与转角。
- 结论：第二轮两项 P2 已关闭，六个构图门槛未回退；但主任务线与红色退回回线仍存在 P1 级曲率偏差。使用 `bezierCurveTo()` 只满足实现形式，尚未精确复现目标轨迹。路线 3 继续 `REWORK`，不得进入总 PASS。

#### 第二轮两项 P2 关闭证据

1. 默认终态 `.e6.current` 已取消附加 outline，实测 `outline-style:none`、边框 `1px`；目标图不存在的双重选中框已消失。
2. `.loom-tools` 默认 `opacity:0`；键盘聚焦 `#prev` 后控件恢复 `opacity:1` 且焦点轮廓可见，兼顾静态贴合与可访问操作。

#### [P1] 三次贝塞尔的分段方式仍不对应目标曲线

- **主任务线**：目标不是跨越两个证据层的大型连续弧线，而是由多组“近竖直直段 → 短半径圆角 → 短水平/斜接段 → 再近竖直直段”组成的阶梯式落线。当前 `258→327`、`382→447` 等段用单个 cubic 跨过直段与转角，视觉上形成大弧或长对角线；目标中定义/执行、执行/评审、评审/整合之间清晰可见的短直段与紧凑转角被抹平。
- **红色退回回线**：目标从评审节点先近水平向右，再以短半径上拐，随后近竖直回到执行层，最后近水平向右到红色端点。当前 `M327 221 C348 221 370 219 370 197 C370 166 417.622 151 448.622 151` 的第一段持续上升、第二段以大缓弧同时完成上移与右移，缺少目标的水平—竖直—水平骨架。
- **绿色通过段**：当前通过段随主线形成长对角线；目标是主线紧凑转角中的局部覆色。必须先修正主线几何，再复用完全相同的局部 path，不能把“控制点相同”当作视觉贴合的替代证据。
- **修复要求**：每个近直线段使用与端点共线的控制点，每个圆角单独使用短 cubic；不得用一个 cubic 同时承担长直段和转角。修后需回传完整 SVG 等价 `path d`、逐段控制点与同尺寸并排图，按切线方向、转角半径和关键节点位置逐项复验。

#### 未回退项

- 标题 `y=100`、织机 `y=278`、收据 `x=1125, y=138`、真人决策条 `y=742`、三声部 `y=849`、页脚 `y=999` 均保持第二轮通过位置。
- 页面在 `1487×1058` 与 `390×844` 下无页面级横向溢出；织机仅保留移动端局部横向浏览。
- 默认控件隐藏、焦点显现、脚本编译、唯一 ID 与控制台检查均通过；本轮驳回仅针对曲线几何，不否定这些闭环项。

#### 当前总裁决

路线 1：`PASS`。路线 3：`REWORK`（曲线 P1 未关闭）。整体精确视觉审计继续待定。

final result: rework

---

## 路线 3 · 第四轮曲线专项复验

### PASS

- 复验依据：最初目标图 `exec-973aad59-ddca-4362-a715-6f038cc415e3.png`、同尺寸全屏图 `artifacts/acceptance/style3-segmented-curve-1487x1058.png`、同尺度曲线并排图 `artifacts/acceptance/style3-curve-target-vs-implementation.png`、实页 `sites/marketeam/style3.html`。
- 结论：第三轮 P1 已关闭。主任务线、红色退回回线和绿色局部覆线均由显式三次贝塞尔分段绘制，直段、短半径转角、关键节点和切线方向与目标一致；没有再出现跨层大弧或长对角线。路线 3 精确视觉复验 `PASS`。

#### 曲线 P1 关闭证据

1. **主任务线分段正确**：主线不再按“每证据层一个大 cubic”绘制，而是拆为近直线段与独立短转角。定义、执行、评审、整合之间依次恢复近竖直落线、短圆角、短水平交接和再次落线的阶梯骨架。
2. **目标骨架像素贴合**：在全图 `x=225–545, y=280–725` 的主线区域，对目标与实现暗色骨架做双向最近像素距离检查：中位偏差 `0 px`，约 `91.9%` 的骨架像素在 `4 px` 内、`98.8%` 在 `6 px` 内，最大偏差 `10 px`。第三轮旧曲线只有约 `52.7%` 在 `4 px` 内、`60.5%` 在 `6 px` 内；本轮不是主观改善，而是可量化收敛。
3. **红色退回骨架正确**：当前回线由五个 cubic 明确分出近水平起步、短半径上拐、近竖直回升、短半径收口和近水平终段；端点约为 `M327 221 → 448.6 151`。同尺度并排图中已恢复目标的水平—竖直—水平关系，顶部与底部关键位置偏差保持在约 `3–5 px`。
4. **绿色通过段复用局部几何**：绿色仅覆盖主线靠近第五节点的短浅局部 `M421 323 C430 324 439 324 447 324`，不再形成跨层长对角线；绿色节点仍锚定第五条证据带。

#### 回归与实现验证

- 标题 `y=100`、织机 `y=278`、收据 `x=1125, y=138`、真人决策条 `y=742`、三声部 `y=849`、页脚 `y=999` 均未回退；全屏证据尺寸为 `1487×1058`。
- 默认终态卡片保持单线边框；推进控件默认不可见，聚焦时可见，第二轮两项 P2 没有复发。
- 两段内联脚本均可编译；15 个 ID 全部唯一；显式分段 `bezierCurveTo()` 主线与回线存在；`git diff --check` 通过。
- 路径数据改动未触碰已通过的布局、响应式规则和事件绑定；作者回归的 `03/06` 退回锁定与 `06/06` 真人决策启用均通过。

#### 总裁决

路线 1：`PASS`。路线 3：`PASS`。本轮精确视觉审计总结果为 `PASS`。

final result: passed
