{
  "phase": "implement",
  "status": "needs-review",
  "change": "terminal-record-member-attribution",
  "completedAt": "2026-08-09",
  "summary": "insertSystemMessage 增加可空 role 并写入；LocalRunFailureRuntime 各入口透传运行角色（primary run.role / worker workerInput.role / dispatch & execution message.dispatchRole / orphan dispatchRole / markStaleRunning 行内派生）；与运行无关的通知保持 null。store 层测试覆盖 role 写入与重启回放；UI 兜底（resolveMessageProcessRole / ?? \"agent\" / 系统提示表头）按 design 保留，operator-console.test.tsx 的「协作者」用例仍是存量 NULL 行的有效覆盖未改写。真实运行 UI 复查与全量闸门待 reviewer / delivery-lead。",
  "known_issues": [
    "与运行无关的 insertSystemMessage 调用点显式传 null；markStaleRunning 的 role 按行派生（role ?? dispatchRole），不新增输入字段。",
    "spec-delta 已写入；归档时按 AGENTS.md 合并回 openspec/specs 并核对 PRD 锚点。"
  ]
}
