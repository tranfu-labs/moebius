# 设置

> 状态：设置容器、常规语言能力与关于分类已确认；其他分类待内容可用后再逐项加入
>
> 所属页面：覆盖主工作区的应用级设置弹窗
>
> 当前交付范围：常规 → 语言；关于 → 应用身份、版本、更新与公开链接
>
> 产品基线：[Moebius 产品需求文档](../prd.md)
>
> 关联入口：[主页面侧边栏](./main-left-sidebar.md)

> **2026-07-27 产品决定：**设置不再作为离开主工作区后进入的独立页面，而改为覆盖当前工作区的居中弹窗。原因是语言等应用级配置应当随时可改，同时保留用户正在进行的项目、对话和输入上下文。首期只显示可用的“常规”分类，提供简体中文与英文；选择新语言时先完成本地持久化，再原子切换整个桌面客户端的界面语言。

> **2026-07-28 产品决定：**设置弹窗采用紧凑、克制的桌面偏好中心结构，去掉重复标题、大块空白和过重的选项卡片；分类导航扩展为“常规”和“关于”。“关于”集中展示品牌身份、当前版本、Apple Silicon 适用范围、检查更新以及发布记录、问题反馈和开源仓库入口。检查更新必须先在弹窗内反馈结果；只有确认存在新版本后，用户再次点击“下载新版本”才打开系统浏览器，不因一次检查动作把用户直接带离应用。

## 页面目标

设置弹窗集中承载应用级配置、应用身份信息和不适合常驻主界面的数据管理入口。用户无需离开当前任务即可在“常规 → 语言”中切换简体中文与英文，或在“关于”中确认自己正在使用的 Moebius 版本并主动检查更新；完成后继续原任务时，工作上下文保持不变。

设置未来也可承接“Agent 与团队”“已移除项目与已归档对话”等分类，但这些分类只有在内容可用时才出现；首期不显示禁用项或“即将推出”占位。

## 用户场景

- 用户希望把 Moebius 的静态界面文案切换为自己更容易阅读的语言。
- 用户排查问题、提交反馈或确认更新前，希望快速查看当前安装版本和适用平台。
- 用户希望主动确认是否有新版本，但不希望单次检查动作未经确认就打开浏览器或开始下载安装。
- 用户在对话或任务进行过程中打开设置，希望修改后原任务和输入上下文不丢失。
- 用户重启应用后，希望继续使用上次选择的语言。

## 入口与去向

- 从[主页面侧边栏](./main-left-sidebar.md)底部“设置”打开居中的设置弹窗。
- 打开弹窗不会改变当前项目、当前对话、对话滚动位置、未发送草稿、Sidebar 开关或项目展开状态。
- 关闭弹窗后回到打开前的工作区位置，并继续原任务。
- 如果弹窗打开期间当前项目已因其他操作变为不可用，关闭后遵循对应页面已有的恢复规则；设置弹窗本身不创建新的导航去向。
- 当前显示“常规”和“关于”两个可用分类；“Agent 与团队”“已移除项目与已归档对话”待内容可用后再出现，不显示禁用项或“即将推出”占位。
- “查看发布记录”“反馈问题”“开源仓库”和确认新版后的“下载新版本”都交给系统浏览器打开对应公开页面；设置弹窗与当前工作区留在原处。

## 页面结构

桌面常规宽度下：

```text
┌ 主工作区（保持原位置，弹窗打开时不可操作）──────────────────────────────┐
│                                                                        │
│          ┌ 设置 ──────────────────────────────────────── × ┐          │
│          │                                                  │          │
│          │  ◉ 常规              语言                        │          │
│          │  ⓘ 关于              选择 Moebius 的界面语言     │          │
│          │                       ┌────────────────────────┐  │          │
│          │                       │ ● 简体中文             │  │          │
│          │                       ├────────────────────────┤  │          │
│          │                       │ ○ English              │  │          │
│          │                       └────────────────────────┘  │          │
│          │                       保存中 / 失败并重试          │          │
│          └──────────────────────────────────────────────────┘          │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

切换到“关于”时，保持同一个弹窗与分类位置，只替换右侧内容：

```text
┌ 设置 ──────────────────────────────────────────────── × ┐
│                                                          │
│  ◉ 常规              [品牌图标]  Moebius                  │
│  ⓘ 关于                         把你在用的 AI，变成一个    │
│                                 会自己推进的团队。          │
│                                                          │
│                       当前版本      0.1.4 [复制版本信息]    │
│                       适用平台      Apple Silicon Mac      │
│                       更新          [检查更新]             │
│                                     已是最新版             │
│                                     新版 0.1.5 [下载新版本]│
│                                     检查失败 [重试]        │
│                                                          │
│                       查看发布记录  · 反馈问题 · 开源仓库   │
└──────────────────────────────────────────────────────────┘
```

窗口较窄时，分类导航移到内容上方；当前分类内容纵向排列，版本、更新结果和操作保持完整可见：

```text
┌ 设置 ───────────────────────────── × ┐
│ 常规   关于                           │
├──────────────────────────────────────┤
│ [当前分类内容纵向排列]                │
│                                      │
│ 当前版本 / 语言选项                   │
│ 更新状态 / 对应操作                   │
└──────────────────────────────────────┘
```

异步操作在弹窗关闭后结束时，当前工作区右下角出现不抢焦点的轻量通知：

```text
┌ 工作区右下角通知 ─────────────────────┐
│ 发现新版本 0.1.5  [打开设置] [关闭通知] │
└────────────────────────────────────────┘
```

## 区域与信息

### 弹窗标题区

- 标题显示当前应用语言下的“设置”。
- 关闭按钮始终可见，并具有与当前语言一致的可访问名称。
- 弹窗打开时，背景工作区保持可辨认，帮助用户确认自己没有离开当前任务；背景内容不可操作。
- 标题区只承担弹窗身份和关闭操作；内容区不再重复显示与当前分类同名的大标题。

### 分类导航

- 当前显示“常规”和“关于”；没有可用内容的后续分类不出现在导航中。
- 默认进入“常规”，当前分类有明确选中态。
- 每个分类使用图标、文字和非仅依赖颜色的选中态；导航保持紧凑，不以大面积底色或独立卡片制造多余层级。
- 切换分类只替换设置内容，不关闭弹窗、不改变背景工作区，也不重置另一分类已经产生的本次反馈。
- 后续分类只有在内容可用并完成各自产品确认后才加入，不以禁用项或“即将推出”占位提前展示。

### 常规

- “语言”作为常规分类中的独立设置项。
- 语言选项显示为“简体中文”和“English”，使用户即使在不熟悉当前界面语言时也能识别目标语言。
- 当前生效的语言必须有清晰、非仅依赖颜色的选中标记。
- 两个语言选项组成一个连续、紧凑的设置组，以行分隔和单一外框表达归属；不再把每个选项渲染成彼此分离的大卡片。
- 没有已保存选择的用户默认使用简体中文，不根据系统语言自动改变默认值。

### 关于

- 顶部品牌区使用产品品牌母版派生的 Moebius 图标、产品名和一句话定位“把你在用的 AI，变成一个会自己推进的团队。”，让用户确认正在查看的应用身份；品牌区不承担营销落地页的完整介绍。
- 版本信息显示当前安装版本，并明确正式适用平台是 Apple Silicon Mac。版本号保持可选择，并提供显式“复制版本信息”操作；复制内容使用 `Moebius <版本号> · Apple Silicon Mac`，不包含内部构建路径、提交标识或本机环境信息。
- 更新区把当前版本、检查动作与最近一次检查结果放在同一信息组中。默认显示“检查更新”；检查过程中显示持续反馈并阻止重复发起。
- 若当前已经是最新正式版本，明确显示“已是最新版”；若存在更新，显示最新版本号和独立的“下载新版本”操作；若检查失败，保留当前版本信息，显示可理解的失败说明和重试入口。
- 底部公开链接提供“查看发布记录”“反馈问题”“开源仓库”。“查看发布记录”进入全部正式发布记录列表，“反馈问题”进入 GitHub 新建 Issue 页面并预填当前 Moebius 版本与 Apple Silicon Mac，“开源仓库”进入仓库首页；三者均明确表现为外部去向，通过系统浏览器打开，不在设置弹窗内嵌网页。

### 语言覆盖范围

- 切换覆盖整个桌面客户端中由 Moebius 提供的静态界面文案，包括页面标题、导航、按钮、菜单、表单标签、占位提示、空态、状态说明、确认与错误提示、悬停提示和可访问名称。
- 同一次界面中不得因组件遗漏而混用简体中文和英文；新增的产品静态文案也必须同时具备两种语言版本后才能进入交付。
- 用户输入、Agent 回复、项目名、对话中引用的文件内容和本地路径保持原文，不随界面语言被翻译或改写。

## 操作与反馈

### 打开与关闭

- 点击 Sidebar 的“设置”后，弹窗在当前主工作区上方打开。
- 用户可以点击标题区关闭按钮或按 `Esc` 关闭；关闭只退出设置，不撤销已经成功保存的语言选择。
- 弹窗打开后，键盘焦点进入弹窗；关闭后，焦点回到触发设置的入口。
- 为避免用户在调整设置时误关闭，点击背景遮罩不关闭弹窗。
- 关闭设置弹窗不取消已经开始的语言保存或更新检查。两类操作的进行中状态与最终结果属于当前应用会话，不依赖弹窗保持打开。
- 操作尚未结束时重新打开设置，必须恢复原进行中状态并继续阻止重复发起；操作已经结束时重新打开，必须显示该操作在当前应用会话中的最近一次结果。
- 弹窗关闭后操作才结束时，当前工作区显示一次不抢焦点的轻量通知：语言操作指向“常规”，更新操作指向“关于”。通知操作可重新打开设置并定位到对应分类；通知关闭或忽略不丢失弹窗内保留的结果。
- 若语言保存成功，整个桌面客户端仍按既定规则切换到新语言，工作区通知使用新语言；若保存失败，客户端保持原语言并在工作区通知中提供重新打开设置的入口。
- 若弹窗始终打开，终态只在弹窗内反馈，不重复显示工作区通知。

### 切换语言

- 用户选择另一种语言后，无需额外点击“保存”或重启应用。
- 应用先把新选择保存到本地；保存成功后，再把设置弹窗和整个桌面客户端原子切换为新语言。正常本地写入下，用户感知为立即生效。
- 页面结构、当前任务和用户内容不因语言切换被重置；下次启动直接使用已保存语言。
- 重复选择当前语言不会产生额外提示或打断用户。
- 若新选择无法持久保存，应用保持此前已保存且当前生效的语言，显示可理解的失败提示，并允许用户重试；不得先切换再回退，也不得表现为已经保存但重启后悄然恢复旧语言。
- 用键盘选择语言后，保存期间焦点保留在所选语言控件；成功或失败只更新该设置组的状态与可访问通知，不把焦点移动到页面开头或关闭按钮。

### 检查更新

- 点击“检查更新”后，按钮进入检查中状态并给出持续可见的进度反馈；同一次检查结束前不得重复发起。
- 一次更新检查必须在 15 秒内进入“已是最新版”“有新版本”或“检查失败”之一；达到上限仍没有结果即按检查失败处理并允许重试，不得永久停在检查中。
- 检查只比较当前安装版本与最新正式发布版本，不自动下载、不自动安装，也不直接打开浏览器。
- 已是最新版时，更新区原地显示成功结果，用户可以继续查看关于信息或关闭设置。
- 有新版本时，更新区显示最新版本号和“下载新版本”。只有用户点击该按钮后，系统浏览器才打开对应正式发布页面；Moebius 保持运行，当前任务和设置弹窗不被替换。
- 检查失败时，应用不得把未知状态表述为“已是最新版”；应显示简短、可理解的失败说明和“重试”，同时保留当前安装版本。
- “查看发布记录”“反馈问题”和“开源仓库”不依赖检查更新成功即可使用。外部页面无法打开时，在当前弹窗中给出失败反馈，不关闭设置。
- 检查按钮在检查中和终态都保持同一焦点位置，不因结果出现而被替换；结果通过可访问状态通知读出。“下载新版本”或“重试”作为同一更新组中的后续操作出现，用户按正常 Tab 顺序即可到达，系统不得自动把焦点抢到新操作上。

### 复制版本与公开链接

- 点击“复制版本信息”后，把当前显示的产品名、版本和适用平台写入系统剪贴板，并在原位置显示可访问的成功反馈；复制失败时保留原内容，显示失败反馈并允许重试。
- “反馈问题”打开 GitHub 新建 Issue 页面时，预填内容必须包含与页面当前显示一致的产品名、版本和适用平台；不预填用户对话、项目名、本地路径或其他工作区内容。
- 打开任何外部页面都不关闭设置弹窗。系统浏览器打开失败时，焦点留在原操作，当前弹窗原地显示失败反馈。

## 页面状态

| 状态 | 表现 | 可用操作 |
| --- | --- | --- |
| 默认 | 打开“常规”，显示当前生效语言 | 切换语言、关闭 |
| 首次使用且无保存选择 | 简体中文为当前语言 | 切换到 English、关闭 |
| 语言切换成功 | 弹窗及整个桌面客户端立即显示目标语言，选中态同步更新 | 继续设置或关闭 |
| 语言保存中 | 标明目标语言正在保存，整个应用继续使用此前语言，避免重复提交造成跳变 | 关闭能力保持可用；重复选择不产生新操作 |
| 语言保存失败 | 整个应用保留此前已保存语言并显示失败提示 | 重试或关闭 |
| 异步操作期间关闭 | 工作区保持可用；操作继续，完成后出现一次不抢焦点的轻量通知 | 从通知重新打开对应分类，或继续当前任务 |
| 重新打开仍在进行的操作 | 恢复原进行中状态，不创建第二次保存或检查 | 关闭或等待 |
| 重新打开已完成的操作 | 显示当前应用会话中最近一次终态 | 使用结果操作、再次检查或关闭 |
| 关于默认 | 显示品牌、当前安装版本、Apple Silicon Mac、检查更新和三个公开链接 | 检查更新、打开公开链接、切回常规、关闭 |
| 更新检查中 | 原地显示正在检查，当前版本保持可见 | 关闭、切换分类；不能重复检查 |
| 已是最新版 | 明确显示当前版本已是最新正式版本 | 再次检查、打开公开链接、关闭 |
| 有新版本 | 显示最新版本号和“下载新版本” | 打开正式发布页、再次检查、关闭 |
| 更新检查失败或超时 | 当前版本保持可见，显示检查失败说明 | 重试、打开公开链接、关闭 |
| 版本复制成功 | 原位置显示“版本信息已复制”的可访问反馈 | 继续设置或打开反馈入口 |
| 版本复制失败 | 版本文本保持可选择，原位置显示复制失败 | 重试或手动选择文本 |
| 外部页面打开失败 | 保留当前设置位置并显示轻量失败反馈 | 重试对应操作或关闭 |

语言资源随桌面应用提供，不出现需要联网下载语言资源的加载态。

## 弹层与危险操作

- 设置本身是主工作区上方的模态弹窗；打开时背景不接受鼠标或键盘操作。
- 弹窗内若将来出现需要二次确认的操作，确认层位于设置弹窗之上，关闭确认层后回到原设置位置。
- 语言切换可逆，不需要危险操作确认。
- 检查更新是只读网络动作；打开发布页、反馈页和仓库不会修改本地数据，不需要确认层。
- 当前范围不在应用内下载或安装更新，因此没有覆盖安装、重启应用或丢失未保存工作的问题。

## 响应式与窗口行为

- 弹窗在桌面常规宽度下居中显示，宽度足以让分类与内容形成清楚的左右层级，但不遮蔽到让用户无法辨认原工作区。
- 窗口变窄时，分类导航移到内容上方；不得压缩到截断当前语言、选项名称、版本号、更新结果或关闭入口。
- 内容超出可用高度时只滚动设置内容，标题与关闭入口保持可达。
- 用户缩放或移动窗口时，当前分类、语言选择、更新结果和滚动位置保持稳定。

## 关联流程

- 从 Sidebar 打开设置、切换语言或检查更新、关闭并继续当前任务，都是本页面内完成的单弹窗旅程，不另建跨页面流程。
- “下载新版本”和三个公开链接只把用户交给系统浏览器，不构成 Moebius 内部跨页面流程。

## 指标与验收

### 产品指标

- **任务闭环：**用户从侧边栏打开设置后，可以在不离开 Moebius 的前提下看到当前版本并得到一次明确的更新检查终态；只有主动选择外部操作时才进入系统浏览器。
- **上下文保持：**打开、切换分类、切换语言、检查更新、复制版本信息和关闭设置均不改变当前项目、对话、草稿、滚动位置与侧边栏状态。
- **结果可信：**每次更新检查在 15 秒内收敛到三个终态之一；失败与超时不得显示成“已是最新版”，同一进行中操作不得重复创建。
- **全键盘可达：**从设置入口到分类、语言、更新、复制和外部操作均有连续的键盘路径；异步状态更新和工作区通知不抢走当前焦点。

### 验收清单

1. 从 Sidebar 点击“设置”会在当前工作区上方打开居中的设置弹窗，不进入独立设置页面。
2. 弹窗默认打开“常规”，分类导航只显示已经可用的“常规”和“关于”，并能明确看到当前语言以及“简体中文”“English”两个选项。
3. 没有保存过语言选择时，应用使用简体中文；选择另一语言并在本地保存成功后，设置弹窗和整个桌面客户端的产品静态文案立即一致切换。
4. 关闭设置或重启应用后，语言选择仍然保留，不需要再次设置。
5. 切换语言不会改变当前项目、当前对话、Sidebar 状态、未发送草稿或用户与 Agent 已产生内容。
6. 用户输入、Agent 回复、项目名、文件内容和本地路径保持原文。
7. 错误、空态、提示、菜单、悬停提示和可访问名称与当前界面语言一致，不出现可见的中英混用。
8. 新语言只在本地保存成功后全局生效；保存失败时旧语言仍处于选中和生效状态，同一语言设置组显示本地化失败消息与重试操作。
9. 弹窗只显示一个“设置”标题；右侧内容不重复“常规”或“关于”标题。两个语言选项位于同一外框内并以行分隔；导航中没有禁用分类、占位分类或为未来内容预留的空白区域。
10. “关于”显示统一品牌图标、Moebius、一句话定位、当前安装版本、Apple Silicon Mac、检查更新以及“查看发布记录”“反馈问题”“开源仓库”。
11. 点击“检查更新”只在弹窗内进入检查状态并反馈结果，不自动打开浏览器、不下载或安装更新；检查期间不能重复发起，并在 15 秒内进入已是最新版、有新版本或检查失败。
12. 已是最新版时给出明确结果；有新版本时显示最新版本号，只有用户点击“下载新版本”后才通过系统浏览器打开正式发布页。
13. 更新检查失败或超时不得伪装成“已是最新版”；当前版本保持可见，同一更新组显示失败消息与重试操作。
14. “查看发布记录”进入正式发布记录列表，“反馈问题”进入预填当前版本与 Apple Silicon Mac 的 GitHub 新建 Issue 页面，“开源仓库”进入仓库首页；三者不依赖更新检查结果即可使用。打开失败时设置弹窗保留原位置并反馈失败。
15. 不显示禁用的未来分类或“即将推出”占位。
16. 当前版本文本可选择；点击“复制版本信息”会复制 `Moebius <版本号> · Apple Silicon Mac` 并给出可访问反馈，复制失败时原位置显示失败与重试。
17. 语言保存或更新检查进行中关闭弹窗不会取消操作；操作终态在工作区显示一次不抢焦点的轻量通知。重新打开对应分类会恢复进行中状态或当前应用会话中的最近一次结果，且不会创建重复操作。
18. 用键盘触发语言保存或更新检查后，焦点留在原控件；终态通过可访问状态通知读出，新增的下载或重试操作可按正常 Tab 顺序到达，系统不自动抢焦点。
19. 在 `900 × 640` 的内容视口中，分类导航位于内容左侧，“常规”和“关于”的主要内容无需页面内部水平滚动即可完整操作。
20. 在 `560 × 640` 的窄内容视口中，分类导航移动到内容上方；语言名称、版本号、更新结果和关闭入口不被截断，弹窗不产生水平滚动。
21. 在 `900 × 480` 的短内容视口中，标题与关闭入口保持可见，只有设置内容区纵向滚动；滚动前后当前分类、语言选择、更新结果和焦点目标保持不变。
22. 仅使用键盘即可打开设置、切换“常规 / 关于”、切换语言、检查更新、复制版本信息、使用结果与公开链接操作、关闭弹窗，并在关闭后回到设置入口。

## 非目标

- 首期不提供简体中文和英文之外的语言。
- 首期不根据 macOS 系统语言自动选择或切换界面语言。
- 不翻译、改写或自动检测用户输入、Agent 回复、项目名、文件内容和本地路径的语言。
- 不在本次交付中补齐“Agent 与团队”的全部设置能力。
- 不在本次交付中定义已移除项目或归档对话的恢复、永久删除和数据保留期限。
- 不提供自动检查频率、自动下载、应用内安装、重启安装、更新渠道或预发布版本选择。
- 不跨应用重启保留更新检查的进行中状态或最近结果；重新启动后“关于”回到未检查状态，但当前安装版本仍正常显示。
- 不在“关于”中展示内部构建路径、提交标识、本机数据目录、运行诊断或依赖版本。
- 不在设置弹窗内承载完整官网、发布记录、问题反馈表单或代码仓库内容。
